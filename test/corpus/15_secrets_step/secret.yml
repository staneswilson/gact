name: secrets-step
on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Authenticate
        env:
          API_TOKEN: ${{ secrets.DEPLOY_TOKEN }}
        run: echo "deploying with token of length ${#API_TOKEN}"
